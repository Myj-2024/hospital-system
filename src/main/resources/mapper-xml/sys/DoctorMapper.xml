<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hospital.mapper.sys.DoctorMapper">

    <resultMap id="UserResultMap" type="com.hospital.entity.sys.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="phone" property="phone"/>
        <result column="identity_card" property="identityCard"/>
        <result column="real_name" property="realName"/>
        <result column="role_id" property="roleId"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 分页查询 -->
    <select id="pageQuery" resultMap="UserResultMap">
        select * from sys_user
        <where>
            <if test="username != null and username != ''">
                and username like concat('%',#{username},'%')
            </if>
            <if test="phone != null and phone != ''">
                and phone like concat('%',#{phone},'%')
            </if>
            <if test="identityCard != null and identityCard != ''">
                and identity_card like concat('%',#{identityCard},'%')
            </if>
            <if test="realName != null and realName != ''">
                and real_name like concat('%',#{realName},'%')
            </if>
        </where>
    </select>

    <!-- 新增用户：核心修复 - role_id直接写死为2（医生角色），不再依赖参数 -->
    <insert id="addUser" parameterType="com.hospital.dto.user.UserDTO">
        insert into sys_user(
            username, password, phone, identity_card, real_name, role_id, status,
            create_time, update_time
        ) values (
                     #{username}, #{password}, #{phone}, #{identityCard}, #{realName}, 2, #{status},
                     NOW(), NOW()
                 )
    </insert>

</mapper>