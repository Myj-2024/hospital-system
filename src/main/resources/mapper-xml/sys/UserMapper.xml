<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hospital.mapper.sys.UserMapper">

    <resultMap id="UserResultMap" type="com.hospital.entity.sys.User">
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="phone" property="phone" />
        <result column="identity_card" property="identityCard" />
        <result column="real_name" property="realName" />
        <result column="role_id" property="roleId" />
        <result column="status" property="status" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <sql id="Base_Column_List">
        id, username, password, phone, identity_card, real_name, role_id, status, create_time, update_time
    </sql>

    <select id="findByUserName" resultMap="UserResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_user
        WHERE username = #{username}
        LIMIT 1
    </select>

</mapper>
